*************************HARI OHM*****************************
Q1. What is JSX ?
Ans: 
    a. syntax alike HTML or XML.
    b. E.g. <h1 id="heading">
                Namstey JSX
            </h1>

Q2. Superpower of JSX ?
Ans: 
    a. can write JS if typed {} inside JSX.
    b. can write reactElement if typed {} inside JSX.
    c. can write reactComponent if typed {} inside JSX.

Q3. Role of type attribute in script tag ? 
    What options can I use there ?
Ans: 
    a. type attribute - type of script indicated by element
        1. a classic script (default)
        2. an import map - JSON object
        3. module - to treat code as JS module
        4. speculationrules - resources that should be prefetched
                                or pre-rendered by browser.
        5. 

Q4. {TitleComponent} --> reactElement
    {<TitleComponent/> or <TitleComponent></TitleComponent>} --> reactComponent

